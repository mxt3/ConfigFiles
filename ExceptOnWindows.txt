# Each line describes a wildcard pattern of a filename that should not be
# symlinked to the home directory on Windows. When specifying the file/directory
# names, leave out the starting '_'. This is assumed.

inputrc
tmux*
fonts
vnc
